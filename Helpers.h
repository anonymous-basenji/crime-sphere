#pragma once
#include <vector>

using namespace std;

class Helpers {
public:
    Helpers();

    /* ========= Print data ========= */
    static void PrintDate(FileProcessing& fileProcessing);
    static void PrintAreaName(FileProcessing& fileProcessing);
};
